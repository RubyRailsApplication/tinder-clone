<div id="nav" class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
  <img class="logo">
  <h5 class="my-0 mr-md-auto font-weight-normal">Tinder clone</h5>
  <%if account_signed_in? %>
    <nav class="my-2 my-md-0 mr-md-3">
      <%= link_to '<a class="text-dark mr-3">Browse</a>'.html_safe, "#"%>
      <%= link_to '<a class="text-dark mr-3">My mathes</a>'.html_safe, "#"%>
      <%= link_to '<a class="text-dark mr-3">Settings</a>'.html_safe, "#"%>
    </nav>

    <%= link_to '<button class="btn btn-link">Sign out</button>'.html_safe, destroy_account_session_path, method: :delete%>

    <% else %>
    <nav class="my-2 my-md-0 mr-md-3">
      <a class="p-2 text-dark" href="#">Features</a>
      <a class="p-2 text-dark" href="#">Support</a>
      <a class="p-2 text-dark" href="#">Pricing</a>
    </nav>

    <%= link_to '<button class="btn btn-primary">Sign up</button>'.html_safe, new_account_registration_path%>
    <%= link_to '<button class="btn btn-link">Sign in</button>'.html_safe, new_account_session_path%>

  <% end %>
</div>